import { PlaceholderPage } from '@/pages/shared/PlaceholderPage';

export default function BatchesPage() {
  return <PlaceholderPage title="Batch Management" step={4} description="Create batches within centres — schedule, level, capacity, fee, assign coaches and students." />;
}
