import { PlaceholderPage } from '@/pages/shared/PlaceholderPage';

export default function SettingsPage() {
  return <PlaceholderPage title="Settings" step={13} description="System settings, feature flags, user management, security rules." />;
}
