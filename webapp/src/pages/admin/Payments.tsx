import { PlaceholderPage } from '@/pages/shared/PlaceholderPage';

export default function PaymentsPage() {
  return <PlaceholderPage title="Payment & Fee Management" step={7} description="Fee dashboard, cash payment recording, overdue tracking, PDF receipts, Razorpay (when live)." />;
}
