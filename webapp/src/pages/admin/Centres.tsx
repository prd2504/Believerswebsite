import { PlaceholderPage } from '@/pages/shared/PlaceholderPage';

export default function CentresPage() {
  return <PlaceholderPage title="Centre Management" step={4} description="Create and manage centres — name, address, maps link, court count, operating hours, sport types." />;
}
