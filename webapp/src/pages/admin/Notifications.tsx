import { PlaceholderPage } from '@/pages/shared/PlaceholderPage';

export default function NotificationsPage() {
  return <PlaceholderPage title="Notifications & Broadcasts" step={10} description="Admin broadcasts, notification preferences, WhatsApp + push delivery." />;
}
