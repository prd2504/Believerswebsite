import { PlaceholderPage } from '@/pages/shared/PlaceholderPage';

export default function SchedulePage() {
  return <PlaceholderPage title="My Schedule" step={4} description="Enrolled batches, weekly timetable, batch details." />;
}
