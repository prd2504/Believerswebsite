import { PlaceholderPage } from '@/pages/shared/PlaceholderPage';

export default function FeesPage() {
  return <PlaceholderPage title="My Fees" step={7} description="Fee status, payment history, receipts, and online payment (when live)." />;
}
